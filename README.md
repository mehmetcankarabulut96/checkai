auto detection of ptohos eighter real or ai generated

run with swagger: localhost:8000/docs

run manuel: post image url as multipart/formdata to localhost:8000/analyze

health check: localhost:8000/health
